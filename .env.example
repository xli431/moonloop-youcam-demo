# Server-side only. Never expose these values to browser code.
# Keep mock mode enabled until the product-specific live adapter is configured.
YOUCAM_API_MODE=mock
YOUCAM_API_TOKEN=
YOUCAM_API_BASE_URL=
YOUCAM_API_PRODUCT=apparel-vto
